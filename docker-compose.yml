version: '3'

services:
  frontend:
    build: .
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/Users/densetsu/free-apps/React/SamplePosts
    command: sh -c 'cd frontend && yarn start'
    ports:
      - '3000:3000'
    tty: true